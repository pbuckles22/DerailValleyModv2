# 13.2.5 walk — recovery playbook

**Captured:** 2026-09-14, before rollback off the pin-board spike.  
**Story:** **13.2.5** Between-car / multi-pickup. Still `[~]`. Never became a new story.  
**Rollback to:** `feature/13.2.5-multi-pickup-desk` @ `e886ec8` then **`2.13.2.5.22.4`** desk Ctrl+Right (cab PASS). UMM **`2.13.2.5.22.4`**.  
**Spike kept (do not delete):** `origin/bug/13.2.5-pin-board` @ walk commit after `40c5810`.  
**Do not:** merge `main`; pop `stash@{0}`; start Epic 15; turn loco/job-car AR on; run #8; guess frog IDs from the dump.

This file is the incremental replay list. Cab eyes win. Core green is not a world frog.

---

## Versioning (why it looks like 13.2.22.31)

There is **no** story `13.2.22`. The string `2.13.2.5.22.34` is:

| Piece | Meaning |
|-------|---------|
| `2` | Major (clean-room rewrite) |
| `13` | Epic 13 |
| `2.5` | Story **13.2.5** |
| `.22.34` | Patch spam on that **same unfinished** story |

`info.json` stayed on **13.2.5** from `2.13.2.5` (2026-09-08) through `2.13.2.5.22.34` (2026-09-14). Time after the desk list PASS was patches, not a new PM story. That is why it feels like wasted clock.

Rollback UMM Version is **`2.13.2.5.22.4`** (22.3 plus desk Ctrl+Right). Spike 22.34 is not in this DLL.

---

## Product lock (engineer bible — do not bypass)

Career SW, job **SL-55**, desk hidden:

1. **#5 Reverse Prep B1S** — couple. Do not Next off Prep.
2. **#6 Forward Past B4L** until CLEARED — this leg, not the next Prep.
3. **#7 Reverse Prep C4S.**
4. After Prep, Maps dest word = **this row’s B4L**, not look-ahead C4S.
5. Yellow pin on the **named B4L dest-side frog** (the far frog that belongs to B4L). Not inbound **1+4**, not B1S **mouth**, not C4S first-stop.
6. Rest facing = bind only. Live facing = Now only.
7. Do **not** run #8 until #6 is a cab PASS you accept.

W1 Maps look-ahead / first-stop C4S as #6 GO is **out**.

---

## Branches

| Branch | Tip (at capture) | Role |
|--------|------------------|------|
| `origin/main` | `12d8d4d` | Last landed: 13.2.4.14 docs. **13.2.5 is not on main.** |
| `feature/13.2.5-multi-pickup-desk` | `e886ec8` | **Rollback here.** W3 harvest + W5 ShowPin Core. Version **22.3**. |
| `bug/13.2.5-pin-board` | `40c5810` + this walk | Spike: remaining-step pin overlay + dest-word + dump last-junction. Cab FAIL. |
| `stash@{0}` | WIP 22.23 on desk branch | **Do not pop.** Older experiments (skip-frog, Len HUD, Rear/laser). |

---

## Walk since `2.13.2.5` (desk list)

Replay **down** this table after rollback. One slice per ship. Cab PASS before the next row. Do not restack the spike in one commit.

### A. On the rollback tip (`e886ec8`, version 22.3) — already in the tree

These landed on `feature/13.2.5-multi-pickup-desk` **before** the pin-board branch. You still have them after checkout.

| Version | Sha | Tried to do | Cab | Replay? |
|---------|-----|-------------|-----|---------|
| `2.13.2.5` | `1c4e66f` | SL-55 two-Prep desk list (B1S then Past B4L then Prep C4S) | List **PASS** | **Keep.** Do not re-prove the list. |
| `2.13.2.5.8` | `16dd45a` | Past-switch Observe dest-side last (C4S not behind first-stop `989976`); HTP crunch opt-in | Cab still due for far C4S `1003160` class | Keep Core. Cab only when you ask. |
| skip-SW | `2bc94b2` | Town crunch skips South Well; TSV must be rebuilt with `FULL=1` | n/a | Keep. Default `dotnet test` = `YMS_FROG_MATRIX_FULL=0`. |
| `2.13.2.5.13` | `21bffc7` | Skip-spent pin; mass kiss; Prep cruise **25** (reverted 5.12’s 10 km/h) | Smoke was due then | Keep unless cab says 10 again. |
| `2.13.2.5.14` | `d76ee9f` | Consecutive Past pins flip F↔R | **PASS** 2026-09-10 | **Keep.** Do not re-prove #6 F / #7 R bind. |
| `2.13.2.5.17` | `4780551` | Prep couple releases handbrakes | **PASS** Handbrakes 0 | **Keep.** |
| `2.13.2.5.18` | `b7fa0ef` | Past-switch CLEARED kiss is `d_stop` only (no 15 m slack) | **PASS** B4L CLEARED | **Keep.** |
| `2.13.2.5.19` | `62fa66b` | Drop extra Past C4S after B4L CLEARED (was re-kissing staging) | Follow-on to 5.18 | **Keep.** Next after CLEARED is Prep C4S, not a second until-CLEARED. |
| `2.13.2.5.22.3` | `36af306` | Dest itinerary W1+W2; list-next **Set** (not Recheck) after spent dismiss | Named-B4L pin **still due** | Keep itinerary Core. Cab pin still the lock. |
| (no bump) | `e886ec8` | W3 first-stop walks; W5 ShowPin re-arms after Dismiss to `990152` / `989976` | Core only | Rollback parent. One live GO pin. No leftover 6/8 overlay. |
| `2.13.2.5.22.4` | (after rollback) | Maps desk **Ctrl+Right** (Insert still works) | **PASS** 2026-09-14 | **Keep.** Do not re-prove the chord. |

**Still open on this tip:** yellow pin on **named B4L** after B1S couple; W5 ShowPin in the cab; story `[x]`.

### B. Pin-board spike (`e886ec8` → `40c5810`) — not in the rollback tree

Cherry-pick **one row at a time** from `bug/13.2.5-pin-board`. Cab PASS or explicit waive before the next.

| Version | Sha | Tried to do | Cab | Replay? |
|---------|-----|-------------|-----|---------|
| `2.13.2.5.22.18` | `79f2b7f` | Dest hops in CI; couple handbrake on any non-Delivery couple; **remaining-steps HUD strip**; **RoutePinBoard** leftover 6/8/1+4 at list-load | 10-row list loaded; strip/pin-board not a named-B4L PASS | **Maybe:** hops + strip **without** `RoutePinBoard`. **Not** the multi-pin overlay until the live GO pin is right. |
| docs | `b920da5` | Sha pin | n/a | Skip. |
| `2.13.2.5.22.27` | `dc30882` | SL-55 default job; Prep latch until stopped; loco/job-car AR **off**; HUD/PID pile-on | **FAIL:** strip on ticker; 6-step bind before TT inject; #6 CLEARED held as Prep S961 | **Maybe:** AR off + Prep latch + SL-55 default. Fix strip Y and 10-row wait separately. |
| `2.13.2.5.22.32` | `5c92ce4` | After-Prep dest **word** = this-leg B4L (not next Prep); Rest facing = bind; overlay under sticky; smoke-hold waits graph + TT inject; engineer-bible rules | Dest word not enough; pin still wrong | **Maybe the rule** (this-leg dest, Rest bind, hold until inject). **Not** frog IDs. |
| `2.13.2.5.22.34` | `40c5810` | Pull-out pin = dump last junction: `#6=1003254`, `#8=990218` | **FAIL** 2026-09-14: log `pin=1003254`, live `latch 990152`, yellow 6 on inbound/lumber throat. `1003254` sits next to mouth `1002868` in the dump. False green. | **Do not replay IDs.** Player names the frog (or harvest while looking at it). |

---

## What was good vs poison

**Keep / cherry-pick later (policy, not pin strings):**

- This-leg Maps dest after Prep = B4L, not next Prep.
- Rest facing = bind; live facing = Now.
- Prep latch while rolling (`SwitchListAutoPrepHold`).
- Loco / job-car AR off until you say on.
- Smoke-hold until graph + TT inject (10-row list, not 6).
- Hide-desk Now/Rest strip **under** the sticky/ticker (layout only).
- Dest hops / hop-coverage CI (`SwitchListHopCoverage`).
- Couple handbrake release (5.17 already PASS; 22.17/18 retest if you touch it).

**Do not cherry-pick as “named B4L”:**

- Golden `1003254` / `990218` / `1002868` as the player frog.
- `RoutePinBoard` + `RoutePinBoardArProbe` as wired (leftover 6/8/1+4 at step 1).
- Test `Smoke_after_Prep_pull_out_pin_is_spur_mouth_not_1plus4_sawtooth` asserting dest-side `1003254`.

---

## Suggested incremental order after rollback

Do **not** start this list until you ask for the next slice. One ship per branch from `e886ec8` / updated desk tip.

1. Cab on **22.4** — after B1S couple, one yellow GO pin. Where is it? Harvest **that** id. No overlay 6/8. Desk chord is already PASS.
2. If dest word is C4S look-ahead: bring **22.32 dest-word only** (this-leg B4L).
3. If you want hide-desk list: **HUD strip only** (no pin board).
4. If you want AR quiet: **22.27 AR off** (small).
5. Pin board / leftover 6/8: **last**, and only after the live GO pin is a cab PASS.
6. Dump last-junction: **never** until the player points at the frog.

---

## How to get a spike file later

```powershell
git show origin/bug/13.2.5-pin-board:YardMasterSuite.Core/RoutePinBoard.cs
git log origin/feature/13.2.5-multi-pickup-desk..origin/bug/13.2.5-pin-board --oneline
```

Cherry-pick a **single** sha onto a **new** `feature/13.2.5-…` branch. Do not merge pin-board wholesale.

---

## Out (still)

Merge `main`. Epic 15. Pop `stash@{0}`. #8 design. W1 first-stop C4S as #6 GO. Delete either 13.2.5 branch.
