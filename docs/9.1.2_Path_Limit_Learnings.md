# 9.1.2 Path Limit look-ahead — learnings + small-wins ladder

**Date:** 2026-08-31  
**Status:** **9.1.2 math locked** (Wins 0–6). **Win 7 cab FAIL** — parked. **Pivot 9.1.3** Core graph walker (path provider only).  
**Current:** **9.1.3 Wins 0–3** local (`2.9.1.26`). Next is **Win 4** Evaluate ← walker (`2.9.1.27`) when user says go. Cruise PID safe on `main` @ `2.9.1.14`; Maps Cruise checkbox default on.

---

## Philosophy (post-thrash)

1. **Rules in Core with named tests** before HUD chrome.
2. **Harvest once, replay forever** — no log archaeology.
3. **No whole-map cache** — one-shot **local subgraph** (≤2.5 km tracks + junctions + boards). Core walks it. Do not dump a pre-traced Unity path as truth.
4. **Check-in each win** so a later miss does not force discarding earlier green Tier 1.
5. **Cab smoke only after** the named HTP Limit walk is green.

---

## Win ladder

| Win | Ship | Version | Cab? | Done when |
|-----|------|---------|------|-----------|
| **0** | Ladder in learnings + PM_PLAN | (with Win 1) | no | Steps written |
| **1** | `CorridorLateralMeters` **12** + synthetic tests | `2.9.1.15` | no | 11.7 m ON; >12 m OFF |
| **2** | Board+path harvest from **SW start** (corridor outward) | `2.9.1.16` | gather once from SW | Codec + dump live; fold `boards-sw-….txt` after gather |
| **3** | HTP eligibility walk (geometry only) | `2.9.1.17` | no | **1398156** on; **1396774** off; throat 50 on + dual |
| **4** | Symmetric junction dual must not govern | `2.9.1.18` | no | 50/50 through → skip; 60/40 still Through |
| **5** | Polarity remaining + same-rail behind-take ~250 m | `2.9.1.19` | no | Reverse travel; far throat 50 cannot take |
| **6** | Evaluate = Maps authority; FILO warm only; HTP Limit walk | `2.9.1.20` | no | Next **40** → Active **40** → Next **60**; never Next=50 |
| **7** | Unity wire + pin smoke | `2.9.1.21`–`.22` | yes | **FAIL** — `TrackPathAhead` truncates; parked. **9.1.3** |

### 9.1.3 Core graph walker (path provider)

| Win | Ship | Version | Cab? | Done when |
|-----|------|---------|------|-----------|
| **0** | Raw graph dump 2.5 km | `2.9.1.23` | gather once | `graph-sw-….txt` tracks+junctions+boards |
| **1** | Graph codec | `2.9.1.24` | no | Parse → Core structs |
| **2** | `CorePathfinder` | `2.9.1.25` | no | Walk 1600 m from loco |
| **3** | HTP routing walk | `2.9.1.26` | no | Path reaches **1402212** 60 |
| **4** | Evaluate ← walker | `2.9.1.27` | no | Same 40→60 Limit gold |
| **5** | Unity wire + pin smoke | `2.9.1.28` | yes | `take 40` then `take 60` |

Win 0 hitch-rides on Win 1 (docs-only alone is not a useful check-in). Stop after each product win until human says continue.

---

## What is safe on `main` (do not confuse with Limit thrash)

| Ship | Version | Verdict |
|------|---------|---------|
| PID speed hold | `2.9.1.12` | On `main` + `origin/main` |
| Takeoff slew / coast / session clear | `2.9.1.14` | On `main` — cruise **23–27 km/h** (mostly 25–27) |

**9.1 cruise is landed.** Limit rebuild does **not** throw away consist hold.

---

## Real wins (ideas to re-apply)

1. **Architecture split** — FILO = warm buffer only; Evaluate = Maps publish authority (`RequireOnPath`).
2. **Path travel polarity** — remaining = `(boardAbs − locoAbs) × sign(travel · segment hint)`.
3. **Far-behind ghost take** — behind take needs **same-rail** + within `TakeAheadMeters` (~250 m).
4. **Harvest** — codec + corridor fixture; HTP must name boards that match geometry.
5. **Log literacy** — chord-bag `T2 limit-ahead` numbers are diagnostics, not Limit. `/skip` ≠ Next.

---

## False paths (do not re-chase)

| Dead end | Why |
|----------|-----|
| Greening HTP on board **1396774** for B4L harvest | ~900 m off corridor / chord-behind |
| Log `40@264` / `60@268` as authority | Off-path chord ghosts |
| Gemini clamp / forward same-rail slack as *the* root cause | Did not green harvest sixty |
| Dual yard-exit queues before single corridor works | Fix one thrown corridor first |
| Cab-debug while HTP names the wrong board | Green walk matching harvest first |
| Stacking **13.1** | Blocked until Limit look-ahead `[x]` (**9.1.3**) |
| Cab-debug `TrackPathAhead` reverse truncate | Win 7 FAIL; replace path provider in Core (**9.1.3**) |

---

## Geometric truth (prior harvest `boards-sw-b4l-2026-08-30.txt`)

Warm nose ≈ `(546.7, 147.4, 591.4)`, fwd ≈ `(0.492, −0.871)`, **9** path segs (runtime often **`n=4`** — short thrown path; live issue for Win 7).

| Id | km/h | Role on *this* dump |
|----|------|---------------------|
| **1398156** | 40 | Real exit. Long-seg chord lat ~**11.7 m**. **8 m rejected; Win 1 12 m accepts (Core).** |
| **1398162** | 50 dual / junction | On corridor (~0.7 m). Steals Next=50 when through — Win 4. |
| **1402212** | 60 dual 60/40 | On corridor, far — HTP “then 60”; **not** 1396774. |
| **1396774** | 60 | **Not on corridor** — never HTP gold for this fixture. |
| **1396790** | 50 dual | Throat ghost — Win 5 behind-take. |

**Harvest design (Win 2) — start at SW, take the corridor with you:** Player gathers from **SW** as the session start (loco there, Maps leave/exit dest, switch thrown, roster settled). Dump is **corridor-scoped**, not yard-scoped: every path segment on the thrown Maps path **plus** posted boards within lookmax (~1600 m) of that path — including mainline / throat / exit boards past the SW yard fence. Header: origin tag `origin SW` (or similar), `pathN=`, `boardN=`, `facingN=`, `dualN=`. Fixture: `boards-sw-….txt`. **Not** full-map cache; **not** “only tracks whose id starts with SW.”

---

## Session fingerprint (do not re-discover)

```text
direction lock · n=5
limit-ahead: … 50@… 40@…/skip 60@…/skip
next=50 … 60@422/skip
# NO take 40 / take 60
```

Player visual on SW leave: **40 then 60**, no 50 on that rail.

---

## Gemini Win 6 (folded)

PASS (2026-08-31) — Evaluate is Maps authority; chord Warm/Tick bypassed for publish.

- Rebuild: `IsOnAnyCorridor` before `InsertByRemaining`; cap discards furthest at `ActiveCapacity`.
- Zero alloc: Evaluate / `LocoAbsOnPath` / `SelectSegmentIndex` — `in` structs, no `new` on hot path.
- `ToSnapshot` FacesTravel skip: no Tick regression; early bail if `!_directionLocked`.
- **1396790** never enters `_slots` (fails 12 m lateral).
- Chord nearest-5 **1396842** / **1402302** (~45–47 m off axis) never reach the funnel; Evaluate overwrites `_slots` with on-path only.
- Segment swap before clamp; `i == 0` keeps negative along (Win 5 polarity).

HTP: Next 40 → Active 40 → Next 60; never Next=50.

### Win 7 must own

Unity wire Evaluate on Maps dest (Tick stays chord off-Maps). Pin smoke: log `take 40` then `take 60`. Hitch not worse. Single-shot harvest (runtime path often `n=4` vs dump `pathN=9`).

Zero-dot polarity = `+1` is accepted as safe.

### Win 7 pin smoke FAIL (`2.9.1.21`) — folded Gemini 2026-09-01

Core held. Cab stopped at pin **CLEARED** with **40 still 296 m ahead** — take cannot fire until the loco passes the board. Sit harvest `pathN=9` had 60; rolling `TryBuild` truncated the long last seg (nearby SW TT pin @~237 m); Evaluate correctly dropped 60. Funnel `n=2` is visible boards, not path hops. Reverse gear was the correct exit.

**Win 7.1 Deep Run — no code.** Keep `2.9.1.21`. Dest **far mainline** (past 60; not SW `#Y` / nearby pin). Same reverse. Drive past 40 then 60; do not stop at first CLEARED.

### 9.1.3 pivot (folded Gemini 2026-09-01)

Keep gate + Evaluate. **Stop** using Unity `TrackPathAhead` as Evaluate's path. Dump raw local graph; `CorePathfinder` in Core; HTP asserts walker reaches **60** before cab. Do not re-prove Wins 0–6 Limit math. Do not mark **9.2** done (**9.2** stays after **13.4**).

---

## Related

- PM_PLAN **9.1.2** walk (blocks **13.1**)
- Fixture (after Win 2): `YardMasterSuite.Tests/Fixtures/Htp/boards-….txt`
- DropZone Gemini stubs: do **not** paste — constant lives on `PostedPathAheadGate`, not a stub `PostedLimitFunnel`
