Receiver brief (13.4 WIP)

**Objective:** Next agent ships **TT midpoint latch** (`2.13.4.15`), then continues yard exit to Prep crash. Do not merge without CMPH.

**Git**

| | |
|--|--|
| **Story** | 13.4 full `[~]` |
| **Version** | `2.13.4.14` |
| **On** | `origin/feature/13.4-yard-chain-1-5` (not merged; `main` still thin `2.13.4.7`) |
| **Do not** | auto-merge; start 15 / 13.2.4; invent full 9.2 MPC mid-cab |
| **Next** | TT midpoint (`2.13.4.15`) → leave → Prep crash → CMPH when exit PASS |

**Decisions:** Sticky `OnTable` until Clear (OffTrack flicker must not re-arm); go-stop snaps thr+indy (bypass DE2 notch expander — was stuck ~18%); train target still **0.90** (HUD ~91%); TT closer from hard brake but not centered — midpoint is next tactical patch; full predictive rem/mass = **9.2** after **13.4**.

**In scope next:** TT midpoint latch + rest of 13.4 exit. **Out:** Epic 15; 13.2.4 couple; auto TT spin; full 9.2.

**Acceptance (partial):** Tier 1 1328 green; Mods `2.13.4.14`; sticky stop-tt + indy 100 cab PASS; **TT midpoint open**; Prep crash open.

**Next steps:** 1) Implement `2.13.4.15` TT center aim (Gemini: ~`L_TT/2`). 2) Smoke midpoint. 3) Spin → Next → Prep crash. 4) Say **CMPH** after full exit PASS.

**Performance**

| Window | This session | Prior | Verdict |
|--------|--------------|-------|---------|
| Spawn | `feature=7` | known | OK |
| Cab (to-TT hard stop) | `feature=0–1` `max=44–78` | H192 `max~65–82` | **not worse** (H193) |
| On-foot look | — | H67/H72 | open |

**Filename:** `docs/handoff/0040-HANDOFF-2026-09-04_0945.md`
