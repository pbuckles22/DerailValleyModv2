Receiver brief (9.1 hold closed)

**Objective:** Next agent starts takeoff-ramp / motor-dead patch only when asked. Do not re-smoke hold-at-25.

**Git**

| | |
|--|--|
| **Story** | 9.1 hold `[x]` (CP1); ramp/motor follow-up open |
| **Version** | `2.9.1.12` |
| **On** | `origin/main` (sha filled after push) |
| **Do not** | re-prove DE2 notch / MUOverride / bleed Hud lock; re-smoke `apply thr=0 indy=27` |
| **Next** | Gradual takeoff + `motors=Dead` when asked; then Epic 13 |

**Decisions:** DE2 HUD notches (0.09/0.18/0.27) not n/11; plant rejects off-grid; `MUOverride` bypasses ControlBlocker; never Hud-round ApproachBrake bleed; thr-off + indy 27 is the hold lock; takeoff 9→100 and motor-dead are patches not 9.2.

**In scope next:** 9.1 takeoff ramp / motor when asked. **Out:** 9.2 MPC; Epic 13 until ramp playable or waived; re-opening 8.7 CLEARED.

**Acceptance (already met):** Tier 1 1137 green; smoke PASS hold + CLEARED; UMM `2.9.1.12`.

**Next steps:** 1) Wait for “ramp” / go. 2) Then `feature/9.1-takeoff-ramp` from this `main`. 3) Harvest thr 9→100 + motors=Dead into named Core tests before cab-debug.

**Performance**

| Window | This session | Prior | Verdict |
|--------|--------------|-------|---------|
| Spawn | `feature=6` `load=0` `max=93` | H156 `feature=4` | same class |
| Cab drive | `feature=0` `max=51–70` | H157 `feature=0` | **not worse** |
| On-foot look | H67/H72 class | H67/H72 | open |

**Filename:** `docs/handoff/0022-HANDOFF-2026-08-30_1318.md`
