# Receiver brief (13.4 thin WIP)

**Objective:** Continue **13.4** smoke on `feature/13.4-autonomous-transit-thin` at UMM **`2.13.4.6`**. Do not start **13.5** or merge to `main` until smoke PASS/waive + **CMPH**.

**Git**

| | |
|--|--|
| **Story** | 13.4 thin `[ ]` WIP |
| **Version** | `2.13.4.6` |
| **On** | `origin/feature/13.4-autonomous-transit-thin` (not merged; `main` still `2.13.6.1` @ `c150590`) |
| **Do not** | auto-merge `main`; start 13.5; Epic 13 close/Release; treat early Job RED take as intended |
| **Next** | Re-smoke 13.4 → CMPH when PASS → then 13.5 |

**Decisions:** Step is end-to-end — prereqs only at step enter (Align if 8.7 Ok + Facing/reverser), never Align on CLEARED end of prior row; CLEARED = stop cue; GO runs current Transit/Pivot only (Prep = Done/Next); smoke auto-**hold** Available job + Load list without TakeJob; TakeJob only on **haul Transit GO after last Prep** (yard Past-switch/to-TT GO must not activate); Stop GO brakes to crawl; GO facing latched for leg; hide purple job AR while Switch List GO; fail-closed Derail ≥65% on Transit GO arm.

**In scope next:** 13.4 cab smoke PASS on `.6` locks above. **Out:** 13.5 auto-stop/drop; 13.3 Validate UI; 13.2.3; 9.2; Nexus.

**Acceptance (not yet):** Tier 1 1320 green; UMM `2.13.4.6` deployed; Tier 2 smoke **not PASS** (reverser-on-enter + early take found before `.6`; Prep GO absence is expected).

**Next steps:** 1) Confirm UMM `2.13.4.6`. 2) World load → `T2 smoke-job: hold` (not early taken). 3) Next steps → Align+F/R prereqs; GO on #4 leave / #6 haul; Done on #5 Prep; take only on #6 GO. 4) User **CMPH** after PASS.

**Performance**

| Window | This session | Prior | Verdict |
|--------|--------------|-------|---------|
| Spawn / cab / on-foot | no hitch-summary this turn | — | — |

**Filename:** `docs/handoff/0038-HANDOFF-2026-09-03_1743.md`

---

## Gates (file only)

- Tier 1: 1320 passed (`dotnet test` Release)
- Reviewer swarm: skipped (not SWAT / not CMPH-to-main)
- Smoke: open — continue on this branch
