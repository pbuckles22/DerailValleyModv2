# Gemini drop — 40 takes, 60 never Next (focused sources)

**Not canonical.** Focused pack (no dump). Replies go in `dropzone/`.

The full snapshot Gemini saw as `Gemini_Snapshot_3.txt` was directory listing + Player.log (Backups). These three files are the live Core sources.

| # | File | Role |
|-----|------|------|
| 1 | `README.md` | This |
| 2 | **`CONTEXT.md`** | Review ask |
| 3 | **`CorePathfinder.cs`** | Walker — fix rolling 1600 m walk dropping 60 |
| 4 | **`PostedLimitFunnel.cs`** | Evaluate consume (Next / take) |
| 5 | **`PostedPathAheadGate.cs`** | Structs + locked 12 m / polarity / dual skip — **do not edit** |

**Upload:** those four with `CONTEXT.md`.  
**Reply:** PASS/WARN/FAIL → `docs/gemini/dropzone/`
